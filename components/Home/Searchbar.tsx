import { View, Text } from "react-native";
import React from "react";
const Searchbar = () => {
  return (
    <View className="bg-red-600">
      <Text className="text-red-500">Searchbar</Text>
    </View>
  );
};

export default Searchbar;
